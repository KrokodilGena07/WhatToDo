import React, {FC} from 'react';
import './Loader.scss';

const Loader: FC = () => {
    return (
        <div className='LoaderContainer'>
            <span className='Loader'/>
        </div>
    );
};

export default Loader;